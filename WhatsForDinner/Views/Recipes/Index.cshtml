@{
  Layout = "_Layout";
}
@using WhatsForDinner.Models;
@model List<WhatsForDinner.Models.Recipe>

@if(Model.Count == 0)
{
  <h3>No recipes have been added yet!</h3>
}
else
{
    <h3 class="day">Breakfast Recipes</h3>
    @foreach(Recipe recipe in Model)
    {
      if(recipe.isBreakfast){
        <p>@Html.ActionLink($"{recipe.Title}", "Details", new {id = recipe.RecipeId})</p>
      }
    }
    <h3 class="day">Lunch Recipes</h3>
    @foreach(Recipe recipe in Model)
    {
      if(recipe.isLunch){
        <p>@Html.ActionLink($"{recipe.Title}", "Details", new {id = recipe.RecipeId})</p>
      }
    }
    <h3 class="day">Dinner Recipes</h3>
    @foreach(Recipe recipe in Model)
    {
      if(recipe.isDinner){
        <p>@Html.ActionLink($"{recipe.Title}", "Details", new {id = recipe.RecipeId})</p>
      }
    }
}
